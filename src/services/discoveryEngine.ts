export function discoverTerms(term:string, mode:'BASIC'|'WORLD'='BASIC'){ const t=term.toUpperCase(); const base=[`${t} VARIANT`, `CINEMATIC ${t}`, `${t} WITH DEPTH`, `PHYSICAL ${t}`, `MOTION-DRIVEN ${t}`, `LIGHT-REACTIVE ${t}`]; const world=['VOLUMETRIC SHADOW','COLD PRACTICAL LIGHT','BRUTALIST NEGATIVE SPACE','GLOSSY BLACK REFLECTIONS','ATMOSPHERIC HAZE','EDITORIAL COMPOSITION','CAMERA-TRACKED DEPTH']; return mode==='WORLD' ? world : base; }
