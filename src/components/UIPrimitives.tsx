import React from 'react';
import { MediaAsset } from '../types';
export const Icon=({name,size=20}:{name:string;size?:number})=><span className="material-symbols-outlined" style={{fontSize:size}}>{name}</span>;
export const SectionLabel=({icon,children}:{icon?:string;children:React.ReactNode})=><div className="section-label">{icon&&<Icon name={icon} size={20}/>}<span>{children}</span></div>;
export const Button=({children,onClick,variant='dark',disabled=false}:{children:React.ReactNode;onClick?:()=>void;variant?:'blue'|'white'|'dark';disabled?:boolean})=><button disabled={disabled} onClick={onClick} className={`btn btn-${variant}`}>{children}</button>;
export const Pill=({children,active,onClick}:{children:React.ReactNode;active?:boolean;onClick?:()=>void})=><button onClick={onClick} className={`pill ${active?'active':''}`}>{children}</button>;
export const Field=({label,value,onChange,placeholder,type='text'}:{label:string;value:string;onChange:(v:string)=>void;placeholder?:string;type?:string})=><label className="field"><span>{label}</span><input type={type} value={value} placeholder={placeholder} onChange={e=>onChange(e.target.value)}/></label>;
export const Select=({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void})=><label className="field"><span>{label}</span><select value={value} onChange={e=>onChange(e.target.value)}>{options.map(o=><option key={o}>{o}</option>)}</select></label>;
export const RefSlot=({label,value,onUpload,onClear,type='image'}:{label:string;value?:MediaAsset|null;onUpload:()=>void;onClear?:()=>void;type?:'image'|'video'})=><div className="ref-wrap"><div className="ref-label">{label}</div><button className="ref-slot" onClick={onUpload}>{value?(value.type==='video'?<video src={value.url} muted/>:<img src={value.url}/>):<Icon name={type==='video'?'video_call':'add_photo_alternate'} size={30}/>}</button>{value&&onClear&&<button className="ref-clear" onClick={onClear}>clear</button>}</div>;
export const Chip=({value,family,onClick,onStar}:{value:string;family?:string;onClick?:()=>void;onStar?:()=>void})=><span className="chip" onClick={onClick}><span className="chip-main">{value}</span>{family&&<span className="chip-sub">{family}</span>}<button onClick={(e)=>{e.stopPropagation(); onStar?.();}}><Icon name="auto_awesome" size={18}/></button></span>;
