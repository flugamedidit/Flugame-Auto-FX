import { CharacterRef } from '../types';
import { Icon, SectionLabel } from './UIPrimitives';
export function CharacterLockPanel({refs,onUpload,onRemove}:{refs:CharacterRef[];onUpload:(files:FileList|null)=>void;onRemove:(id:string)=>void}){ return <div className="charlock"><div className="char-head"><SectionLabel icon="person">Character Lock</SectionLabel><div className="mini-toggle"><b>Existing</b><span>Create Ref</span></div></div>{refs.length===0?<label className="char-empty"><input type="file" accept="image/*,video/*" multiple onChange={e=>onUpload(e.target.files)}/><Icon name="person_add" size={32}/><span>Load Identity Anchors</span></label>:<div className="char-grid">{refs.map(r=><div className="char-card" key={r.id}><img src={r.url}/><b>SUB {r.subject}</b><button onClick={()=>onRemove(r.id)}>delete</button></div>)}<label className="char-add"><input type="file" accept="image/*,video/*" multiple onChange={e=>onUpload(e.target.files)}/><Icon name="add"/><span>Add More</span></label></div>}</div> }
